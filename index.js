/**
 * This is the main entry point for Forge, and is used
 * to build the default `forge.css` and `forge.js` bundles
 * used by client applications.
 */

// Our stylesheet entry point, which builds the
// CSS for patterns included in Forge.
import './scss/forge.scss';

// Our script entry point, which builds JavaScript
// behaviors for patterns included in Forge.
import './js';

